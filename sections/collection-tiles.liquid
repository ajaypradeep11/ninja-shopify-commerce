<section class="container-wide section-pad">
  <div class="section-heading">
    <h2>{{ section.settings.heading }}</h2>
    {%- if section.settings.link_label != blank -%}
      <a class="tiles-link" href="{{ section.settings.link | default: routes.collections_url }}">{{ section.settings.link_label }}</a>
    {%- endif -%}
  </div>
  <div class="collection-tiles">
    {%- for block in section.blocks -%}
      {%- liquid
        assign tile_collection = block.settings.collection
        if tile_collection == blank and block.settings.title != blank
          assign guess = block.settings.title | handleize
          if collections[guess] != blank
            assign tile_collection = collections[guess]
          endif
        endif
        assign tile_image = block.settings.image
        if tile_image == blank
          assign tile_image = tile_collection.featured_image
        endif
        if tile_image == blank and tile_collection.products.size > 0
          assign tile_image = tile_collection.products.first.featured_image
        endif
        assign tile_subtitle = block.settings.subtitle
      -%}
      <a class="collection-tile" href="{{ tile_collection.url | default: routes.all_products_collection_url }}" {{ block.shopify_attributes }}>
        <span class="collection-tile__media">
          {%- if tile_image != blank -%}
            {{ tile_image | image_url: width: 900 | image_tag: loading: 'lazy', widths: '320, 480, 700, 900', sizes: '(max-width: 639px) 66vw, 20vw', alt: tile_image.alt }}
          {%- endif -%}
        </span>
        <span class="collection-tile__footer">
          <span class="collection-tile__title">{{ block.settings.title | default: tile_collection.title | default: 'Collection' }}</span>
          {%- if tile_subtitle != blank -%}
            <span class="collection-tile__subtitle">{{ tile_subtitle }}</span>
          {%- endif -%}
        </span>
      </a>
    {%- endfor -%}
  </div>
</section>

{% schema %}
{
  "name": "Collection tiles",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Shop by category"
    },
    {
      "type": "text",
      "id": "link_label",
      "label": "Link label",
      "default": "View all →"
    },
    { "type": "url", "id": "link", "label": "Link" }
  ],
  "blocks": [
    {
      "type": "collection",
      "name": "Collection",
      "settings": [
        { "type": "collection", "id": "collection", "label": "Collection" },
        { "type": "image_picker", "id": "image", "label": "Custom image" },
        { "type": "text", "id": "title", "label": "Custom title" },
        {
          "type": "text",
          "id": "subtitle",
          "label": "Subtitle",
          "info": "Leave blank to show the collection's lowest price automatically."
        }
      ]
    }
  ],
  "max_blocks": 8,
  "presets": [
    {
      "name": "Collection tiles",
      "blocks": [
        { "type": "collection" },
        { "type": "collection" },
        { "type": "collection" },
        { "type": "collection" }
      ]
    }
  ]
}
{% endschema %}
