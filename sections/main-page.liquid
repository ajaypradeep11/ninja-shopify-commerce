<section class="container-reading page-section">
  <h1 class="page-title">{{ page.title }}</h1>
  {%- if page.content != blank -%}<div class="rte">{{ page.content }}</div>{%- endif -%}
</section>

{% schema %}
{
  "name": "Page"
}
{% endschema %}

